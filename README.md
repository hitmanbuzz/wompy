# Wompy (GUI Version)

A small chat-app hobby project made in rust
<br>
It is kinda fuckup and i am taking a break from it. The non gui version works but there are many parts such as `groups` `users` which are not implemented properly.

### Server
```
cargo run --bin server
```

### Client
```
cargo run --bin client
```

- Made by `Moirangthem Henthoiba` (hitmanbuzz)
